#!/usr/bin/StolenScript
# Variables
let alfa = 100
let sigma = "two"
let beta = 25
let alfa = alfa + 1
let sigma = 501
let beta = beta * 2

write(sigma)
write(alfa)
write(beta)


# Conditionals
IF sigma == 502 THEN let beta = "sigma is 502!" ELIF sigma == 501 THEN let beta = "sigma is 501!" ELSE let beta = "sigma is 500!"

write(beta)


# Loop
FOR i = 0 TO 5 THEN
	write("Follow to @elk.yw")
END
