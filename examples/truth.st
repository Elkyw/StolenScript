# Truth Machine
# If the input is true (a 1), then print true forever
# If the input is false (a 0), then print false once
let truth = input_int()
IF truth THEN WHILE true THEN write(true)
IF NOT truth THEN write(false)
