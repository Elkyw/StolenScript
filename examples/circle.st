#!/usr/bin/StolenScript
write ("radius of the circle :")
let r = input_int()
let pi = 3.14
let area = pi * r * r
let perimeter = 2 * pi * r
let volume = 4 * pi * r * r
write ("area:")
write (area)
write ("perimeter:")
write (perimeter)
write ("volume:")
write (volume)