#!/usr/bin/StolenScript
# This program calculates the perimeter and area of a rectangle
write("")
write("Calculate information about a rectangle")
write("length:")
let length = input_int()
write ("width:")
let width = input_int()
let area = length * width
let perimeter = 2 * length + 2 * width 
write ("Area :")
write ( area )
write ("Perimeter :")
write (perimeter)
